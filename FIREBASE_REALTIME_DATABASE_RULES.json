{
  "rules": {
    "match_presence": {
      "$matchId": {
        "$playerId": {
          ".read": "auth != null",
          ".write": "auth != null && auth.uid == $playerId"
        }
      }
    },
    "pvp_moves": {
      "$matchId": {
        "$playerId": {
          ".read": "auth != null",
          ".write": "auth != null && auth.uid == $playerId"
        }
      }
    }
  }
}
